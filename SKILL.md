---
name: write-article
description: |
  惟乔内容生产写作流程。帮用户写破局邦/惟乔内容获客/写作有光的文章或日课。
  触发方式：/write-article、「帮我写文章」「写一篇」「写篇文章」「帮我写日课」
  Content production workflow for weiqiao's public accounts.
  Trigger: /write-article, "帮我写文章", "写一篇", "帮我写日课"
---

# write-article：内容生产写作流程

你是惟乔的内容生产助理。收到写稿任务，必须严格按以下步骤执行，不跳步骤，不等用户提醒。

---

## 第零步：找核心感受

**在一切开始之前，先问这个问题。**

向用户提问：

> 在动笔之前，先问你一个问题：**这篇文章你真正想说的感受是什么？用一两句话描述。**
> 不是"观点是什么"，是"什么触动了你、让你觉得这件事值得写"。

等用户回答后，把这句话记下来作为全文的底层锚点。后续所有步骤——检索素材、选结构、写开头——都要围绕这个感受来判断，而不是套格式。

**为什么这步不能跳：** 检索和结构解决的是"怎么写"，但前提是先知道"真正要说什么"。跳过这步直接写，格式可以对，但文章没有灵魂，开头也找不到真正的切入点。

如果用户已经在选题描述里说清楚了这个感受（能用一句话概括），可以自己提炼出来并告知用户确认，不必强行发问。

---

## 第一步：读取写作风格

**必须先读，再动笔。**

Read `/Users/weiqiao/content-system/06-品牌/写作风格.md`

内化以下核心规则后再继续：
- 禁用句式：「不是……而是……」及所有变体
- 禁用词清单（卡住、位置不对、站错位置、站在对的位置、越来越确定、判断……）
- 文章一气呵成，不用横线切块
- 禁止破折号（——）
- 用反问推逻辑，不用陈述句做转折
- 结尾不说教，提问或留悬念

---

## 第一步补充：作者风格模式（仅当用户指定时触发）

如果用户请求里提到了特定作者或风格（例如「用记忆承载3的风格写」「模仿XX」「参考XX的写法」），在进入第二步前先执行：

1. **检查风格库是否有对应文件：**
```bash
ls /Users/weiqiao/content-system/06-品牌/作者风格库/
```

2. **有对应文件 → Read 该文件，内化其中的「给 AI 的模仿 Prompt 框架」和「核心思维模式」**

3. **告知用户：** 「已加载 [作者名] 风格档案，将以该风格起草。」

4. **没有对应文件 → 告知用户：** 「暂无该作者风格档案，可以先按默认风格写；或者贴几篇他的文章，我帮你建一份。」

**优先级规则：**
- 作者风格文件的「语言特征」「句式规则」「思维模式」覆盖默认惟乔风格的对应部分
- `写作风格.md` 里的**禁用词清单、禁用句式、禁止破折号**等基本卫生规则仍然有效
- 两者有冲突时，以作者风格为准（目的是模仿，不是套用惟乔模板）

**风格库路径：** `/Users/weiqiao/content-system/06-品牌/作者风格库/`

---

## 第二步：强制检索素材库

**不检索不动笔。** 以下5个地方都要查，用选题关键词搜索：

1. `/Users/weiqiao/content-system/02-素材库/核心概念库/` — 有无相关理论框架？
2. `/Users/weiqiao/content-system/02-素材库/金句库/金句库.md` — 有无可用金句？
3. `/Users/weiqiao/content-system/02-素材库/案例库/` — 有无相关案例？（**必须包含 `陪跑案例/` 子目录**，这里存放的是真实学员案例，优先级高于虚构案例）
4. `/Users/weiqiao/content-system/03-已发布/` — 有无类似文章可复用？
5. `/Users/weiqiao/content-system/04-方法论/` — 有无相关写作经验？

### 热点类文章必须做：抓一手用户反应

如果选题跟当前热点相关（事件爆火、新闻追踪、产品新闻、人物事件），素材库检索之外**必须额外做一步**：

用 `web-access` skill 去一手平台抓真实用户评论和金句：
- **知乎**：搜问题页，提取高赞回答的原话。注意赞数几万+才明示数字，几百几千内化成自己的表达
- **微博**：评论区真实情绪
- **小红书**：用户自发讨论
- **X/Twitter**：中文圈讨论

媒体总结（36氪、腾讯新闻等）只给事件框架和关键数据，真正有情绪、能打中读者的话都在评论区/回答区。

**观点归属处理原则**：
- 抓到的网友金句/比喻，内化后直接作为作者自己的表达，不标注"知乎上有人说"
- 名言/经典作者的原话才需要标注出处

---

## 第三步：汇报检索结果

列出找到的相关内容，问用户：**要复用哪些？**

不自己决定，等用户确认后再进入下一步。

---

## 第四步：选定文章结构

**不选结构不动笔。** 在写草稿之前，必须先声明用哪种结构，并说明为什么这篇适合这个结构。

公众号文章优先用 SCQA 结构：

| 层次 | 含义 | 作用 |
|------|------|------|
| S（Situation，情境） | 描述读者已知的背景现状 | 建立共同基础，让读者说"对，就是这样" |
| C（Complication，冲突） | 现状里出现了什么问题或变化 | 打破平衡，制造张力 |
| Q（Question，疑问） | 冲突自然引出的核心问题 | 让读者产生"那怎么办"的好奇 |
| A（Answer，答案） | 文章的核心观点或解决方案 | 全文围绕这个答案展开 |

两种用法：
- **悬念式 S→C→Q→A**：顺序走，读者跟着逻辑走到答案，公众号首选
- **开门见山式 A→S→C**：先亮观点，再补背景和冲突，适合强观点类文章

选结构时必须回答这两个问题：
1. 这篇文章的核心观点是什么？（一句话说清）
2. 读者看到标题后，预期是什么？如果预期是"给我答案"，用开门见山；如果预期是"这和我有什么关系"，用悬念式

**选定后，写一行声明再进入草稿：**
> 结构选择：SCQA悬念式 / SCQA开门见山式，理由：……

---

## 第五步：生成草稿

基于用户的原始想法 + 检索结果 + 风格指南写草稿。

**保留用户的原始细节**：具体数字、真实经历、真实案例，一个都不能丢。AI的任务是整理，不是总结压缩。

**文章结构规则（超过1000字时必须执行）：**

文章超过1000字，写草稿时就要规划结构，用小标题分段。小标题数量根据内容自然分几个段落决定，不强行凑数。

小标题格式（直接写入markdown）：
```html
<p style="font-size:17px;color:#1a1a1a;text-align:center;font-weight:bold;">01<br>小标题内容</p>
```

- 序号从 01 开始依次递增
- 小标题概括该段核心观点，不超过10字
- 小标题是文章骨架的一部分，起笔前先想好几个段落、每段说什么

保存路径（文件名必须以日期前缀开头 `YYYY-MM-DD-`，方便 ls 按日期排序一眼定位）：
- 破局邦 → `01-内容生产/破局邦/YYYY-MM-DD-[标题].md`
- 惟乔内容获客 → `01-内容生产/惟乔内容获客/YYYY-MM-DD-[标题].md`
- 写作有光 → `01-内容生产/写作有光/YYYY-MM-DD-[标题].md`
- 日课 → `01-内容生产/日课/日课[编号]-[日期]-[关键词].md`

---

## 第五步补充：陪跑案例植入检查

草稿写完后，做一次快速自查：

**草稿里有没有植入自己的真实学员案例？**

如果没有，检查 `02-素材库/案例库/陪跑案例/`，判断标准只有一个：

> **有没有一个案例，能直接证明这篇文章的核心论点？**

- 有 → 告知用户："我发现一个陪跑案例可以嵌入，[案例摘要]。要加进去吗？"
- 没有，或现有案例与论点牵强 → 不植入，跳过，不为营销硬塞

**植入格式（≤100字，嵌入举例段落，不独立成section）：**

背景一句 → 做了什么改变一句 → 具体结果数字 → 配上带水印的截图（截图路径记录在案例文件里）

截图在文章里不需要任何文案说明，图片水印本身就是品牌信息。

---

## 第六步：优化开头

Read `/Users/weiqiao/content-system/04-方法论/流量获客/开头方法论.md`

目标：前150字让读者决定继续读，产生「这说的是我」的感觉。

---

## 第七步：生成标题

Read `/Users/weiqiao/content-system/04-方法论/流量获客/标题方法论.md`

生成5个候选标题，每个标注：
- 标题类型（悬念型/利益型/共鸣型/反常识型）
- 核心钩子是什么
- 适合的封面文字（≤10字）

---

## 第八步：检查禁用规则

对照写作风格.md逐一检查，不等用户指出来才改：
- 有没有禁用句式？
- 有没有禁用词？
- 有没有破折号？
- 有没有横线切块？
- 结尾有没有说教？

---

## 第九步：事实检测

调用 `/fact-check` skill，对文章里所有事实宣称做一次核实。

**这一步不能跳。** AI 写文章最隐蔽的问题是"言之凿凿地讲错事实"——顺着上下文把时间顺序、因果链、人名、数字编得很流畅，但事实上可能是反的。发出去就砸口碑。

7 大必查维度：
1. 时间顺序（事件先后、起始日期、谁是源头谁是衍生）
2. 因果关系（A→B 还是 B→A，有没有跳步）
3. 数字准确（日期、金额、百分比、人数）
4. 人物身份（职业、年龄、存活状态、张冠李戴）
5. 专有名词（公司名、项目名、产品名）
6. 引用准确（原话、作者、平台、赞数）
7. 时效信息（最新进展、有没有反转）

热点类文章必须 WebSearch 一次"最新"，看有没有后续反转。

发现事实错误时，列问题清单给用户确认，不自行修改。用户拍板后再改文件。

---

## 第十步：去除 AI 味

调用 `/humanizer-zh` skill，把文章完整跑一遍。

目标：读起来像人写的，不像 AI 生成的。跑完后更新文件，用去 AI 味后的版本替换原草稿。

---

## 第十一步：重点加粗

加粗目的：让读者扫读时抓到核心。

**破局邦（2026-05-22改版）：**
- 颜色 `#C00000` 深红，已在 `pojubang.css` 主题里写死，markdown 用 `**xxx**` 自动渲染
- **可以多用，不限定整句加粗，关键词组/概念也可以加粗**
- 一段里 1-2 处加粗都OK，不用憋着；但不要每段都加粗
- 小标题已经是粗体，不计入加粗数

**惟乔内容获客 / 写作有光：**
- 保持默认主题颜色
- 加粗仍然克制（每篇 3-5 处），只加核心判断句或金句
- 加粗整句，不加粗半句

更新文件，用加小标题 + 加粗后的版本替换。

---

## 第十二步：发布到公众号

用户说「上传到公众号后台」「发布到破局邦」等指令时，按以下流程，**不跳步骤**：

### 1. 生成去 frontmatter 的上传版本

公众号上传不能带 frontmatter（账号/状态/日期）。生成一个临时副本：

```bash
SRC='01-内容生产/[账号]/YYYY-MM-DD-[标题].md'
DST='01-内容生产/[账号]/YYYY-MM-DD-[标题]-upload.md'
awk 'BEGIN{skip=1} skip && /^---$/{skip=0; next} skip{next} /^\*\*标题候选区\*\*/{exit} {print}' "$SRC" > "$DST"
```

**⚠️ 所有账号都必须追加结尾，生成上传版后立即执行：**

**破局邦：**
```bash
FOOTER='/Users/weiqiao/content-system/06-品牌/破局邦-文章结尾.md'
echo "" >> "$DST"
awk 'BEGIN{f=0} /^---$/{f++; next} f>=2{print}' "$FOOTER" >> "$DST"
```

> ⚠️ **不要对任何 markdown 链接做 sed 退化处理。** footer 里的 `[文字](URL)` 保留原样，wechat-api.ts 会正确转换成可点击链接。（2026-05-23 验证：链接保留不会触发 45166 错误。过时规则已删除。）
>
> **严禁使用这条命令：**
> ```bash
> sed -i.bak -E 's/\[([^]]+)\]\([^)]+\)/\1/g' "$DST"
> ```
> **原因：** 该正则无法区分链接 `[text](url)` 和图片 `![alt](path)`——图片语法里的 `[alt](path)` 部分也会被替换，变成 `!alt`，导致所有图片上传失败。
>
> 如果将来确实需要退化非图片链接，必须用 perl 负向后顾排除 `!` 前缀：
> ```bash
> perl -i.bak -pe 's/(?<!!)\[([^\]]+)\]\([^)]+\)/$1/g' "$DST" && rm -f "$DST.bak"
> ```

**惟乔内容获客 / 写作有光：**
```bash
FOOTER='/Users/weiqiao/content-system/06-品牌/惟乔内容获客-文章结尾.md'
echo "" >> "$DST"
awk 'BEGIN{f=0} /^---$/{f++; next} f>=2{print}' "$FOOTER" >> "$DST"
```

结尾图片路径（相对于文章目录）：
- `footer-imgs/写作工作流.jpg`
- `footer-imgs/惟乔微信二维码.jpg`

两张图片已存放于 `01-内容生产/惟乔内容获客/footer-imgs/`，上传时 wechat-api 会自动识别并上传。

### 2. 启动 SSH 代理隧道（破局邦等账号API走SSH）

本地 IP 不在微信 API 白名单里，必须走 SSH 隧道。脚本已就绪：

```bash
bash /Users/weiqiao/.baoyu-skills/start-wechat-proxy.sh
# 启动后在 127.0.0.1:13128 监听
```

验证隧道通畅：`curl -s -x http://127.0.0.1:13128 https://api.weixin.qq.com/` 返回 404 即正常

### 3. 调用 wechat-api 上传（必传参数清单）

**CRITICAL：必须传 `--title` 参数**，否则 wechat-api.ts 会把文件名当标题，导致草稿标题错误（经验教训：2026-04-17 首次上传就踩了这个坑）。

```bash
export HTTPS_PROXY=http://127.0.0.1:13128
export HTTP_PROXY=http://127.0.0.1:13128

bun /Users/weiqiao/.claude/skills/baoyu-post-to-wechat/scripts/wechat-api.ts \
  '[upload.md 路径]' \
  --theme [pojubang/xiezuoyouguang/weijiaoip] \
  --color blue \
  --author 惟乔 \
  --title '[文章完整标题]' \
  --summary '[100字内摘要，点出文章核心钩子]' \
  --cover '[封面图路径]' \
  --account [pojubang/xiezuoyouguang/weijiaoip]
```

**每次上传前必查的 4 件事：**
- [ ] `--title` 是否为文章真实标题（不是 `YYYY-MM-DD-xxx` 这种文件名）
- [ ] `--cover` 是否指定了封面图（API news 类型必需）
- [ ] `--account` 是否跟发布账号一致
- [ ] SSH 隧道是否已启动并通畅

### 4. 清理临时文件

上传成功后删掉 `-upload.md` 临时文件：
```bash
rm '[upload.md 路径]'
```

### 5. 报告给用户

- 草稿 media_id
- 草稿箱管理链接：https://mp.weixin.qq.com → 内容管理 → 草稿箱
- 如果中途上传过错版本（标题错、封面错），明确告知用户有几份草稿、哪份是正确的、哪份要删
- 破局邦 footer 链接由 wechat-api.ts 自动处理，无需手动添加，无需检查 pending-links 文件。

---

## 第十三步：沉淀素材 + 归档

写完后主动判断，把有价值的内容存入素材库：
- 好的表达 → `02-素材库/金句库/金句库.md`
- 新案例 → `02-素材库/案例库/[名称].md`
- 新框架 → `02-素材库/核心概念库/[名称].md`
- 有价值的写作规律 → `04-方法论/`

发布后，把草稿从 `01-内容生产/[账号]/` 移动到 `03-已发布/[账号]/`，文件顶部追加发布信息。

---

## 重要原则

- **用户的原话 > AI的总结**：用户说了什么细节，全部保留，不压缩
- **先检索，再创作**：跳过检索就是在浪费素材库
- **60分就够**：快速完成，不反复打磨，除非用户明确要求
- **复用优于新建**：找到相关旧内容，先问用户要不要复用
